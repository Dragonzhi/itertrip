# IterTrip · 设计文档

> 状态：v1.0 方向定稿 · 2026-09-01 · 定位变更：独立 Web 应用（脱离 Skill 附庸）
> 产品一句话：**把你从任何地方看到的旅游攻略，变成一张可以动手改、可以带走的地图。**

---

## 1. 定位与叙事

### 1.1 产品定位（v1.0 变更）

旧定位（已废弃）：「AI 帮你规划旅行」——红海，与携程问道/飞猪问一问正面竞争，无胜算。

新定位：**攻略的结构化落地工具**。用户在任何内容平台（小红书/公众号/抖音/B站）刷到攻略，
人肉流程是「收藏 → 开地图逐个搜 → 自己排顺序 → 发现动线乱」。IterTrip 把这一步变成：
**贴进对话框 → 得到结构化路线 → 地图直观呈现 → 动手修改 → 导出带走**。

### 1.2 为什么有竞争力

- **大厂不占位**：OTA 的 AI 生成自家行程包，绝不会解析竞品平台的攻略——中立落地工具是空位
- **LLM 能力对齐**：「非结构化 → 结构化」恰是大模型成本最低、成功率最高的任务；攻略里的地点已被作者验证过，比凭空规划可靠
- **差异化在生成之后**：市面 AI 规划器全是「生成一次就死」的静态结果；本产品的核心闭环是
  **对话改路线（AI 动手）+ 编辑器（用户把关）+ 撤销栈（随时反悔）**——这个组合目前基本无人做
- **产物护城河**：自包含 HTML 导出（可编辑版）是既有招牌能力，分享即产品体验

### 1.3 形态决策（已定）

| 决策 | 结论 | 理由 |
| --- | --- | --- |
| 产品身份 | **独立 Web 应用** | 不再是任何 Agent/Skill 的附庸 |
| 部署形态 | **本地优先（C-1 单进程）** | start.ps1 一键起；断网可演示；将来可选上云 |
| LLM 配置 | **BYOK + 设置面板** | 用户 key 存 localStorage；后端代理调用（规避 CORS 碎片化）；env 作兜底默认值 |
| 输入方式 | **降级矩阵**（见 §3） | 贴文字/截图是主路径，链接解析是可选彩蛋 |
| 模型策略 | **单多模态模型全包** | 一个 VLM 干「攻略理解 + 提取 + 对话改路线」全部工作，不做规划/视觉双模型 |
| 抓取边界 | 用户自己的会话/key/账号 | 工具只是壳；不内置任何平台凭据 |

---

## 2. 核心体验流

```
用户打开应用
    ↓
对话框：「想去成都 3 天」 / 贴一段小红书攻略文字 / 丢一张攻略截图 / 贴一个链接
    ↓
AI 理解意图 → 生成/提取 route JSON（schema 见 §5）
    ↓
地图直观呈现（图钉 + 按天色板 + 有向路线 + 比价卡）
    ↓
两条修改路径并存：
  ① 对话改：「第二天太赶，博物馆挪到第一天下午」→ AI diff route → 即时重排
  ② 手动改：拖拽排序 / 跨天移动 / 编辑表单 / 地图改点 / 撤销重做（已实现）
    ↓
导出：自包含可编辑 HTML / route JSON
```

**关键交互原则**：AI 是动手的，用户是把关的。AI 的每次修改走同一历史栈，可撤销；
AI 修改前在对话里说明「我改了什么」，地图高亮变化处。

---

## 3. 输入降级矩阵（可靠性设计核心）

| 优先级 | 输入方式 | 实现 | 可靠性 |
| --- | --- | --- | --- |
| 主路径 ① | 粘贴攻略文本 | 纯文本 → LLM 提取 | 永不坏 |
| 主路径 ② | 上传截图 | VLM 直接看图出 route JSON（一次调用，不做图→文中转） | 稳定（依赖 vision 模型） |
| 兜底 ③ | 纯对话 | 意图收集 → 生成 | 永不坏 |
| 彩蛋 ④ | 贴平台链接 | 三层降级：a) 匿名 fetch 分享短链；b) 失败→提示改用 ①②；c) 可选 resolver 插件（Playwright + 用户扫码，本地缓存会话，参考 MediaCrawler 思路但**仅作可选扩展、不进核心依赖**——其学习用途 license 不允许打包分发） | 随平台风控波动 |

**红线**：④ 永远是 best-effort。爬虫被掐死时，产品一条腿都不断。

---

## 4. 模型与 BYOK 设计

### 4.1 设置面板（前端，localStorage 持久化）

- 字段：Base URL / API Key / 模型名（下拉 + 自由输入）+「测试连接」按钮
- 测试连接 = 1 token 补全探测；顺带探测**是否接受图片输入**，不接受则置灰截图入口
- 推荐 VLM 优先（DeepSeek 无视觉，默认供应商将变更；候选：Qwen-VL 系 / GLM-V 系 / GPT-4o / Gemini-Flash 系）
- 优先级：UI 配置 > env 兜底（ITERTRIP_LLM_*）> mock 降级（现有链路保留）

### 4.2 调用链路

前端请求头带 `X-LLM-Base / X-LLM-Key / X-LLM-Model` → 本地后端代理转发。
为什么不让浏览器直连供应商：CORS 政策每家不同且随时会变（Anthropic 需专用头、DeepSeek 未承诺），
后端代理用 OpenAI 兼容格式通吃，planner.py 已验证。key 虽经过本地进程但不出用户机器。

### 4.3 上下文与成本护栏

1. 截图提取完成后，原图从后续轮次上下文丢弃（只留结构化结果），防 token 膨胀
2. JSON 校验失败 → 带错误重试一次（pydantic 校验即修复环地基）
3. 对话历史裁剪：只保留最近 N 轮 + 当前 route 快照，不无限累积

---

## 5. 数据模型（不变项）

route JSON 契约维持现状（trip / days[] / places[] / hotel / summary），
它是「生成、编辑、导出」三端共享的唯一数据源，也是 AI 对话修改的操作对象。
新增可选字段（向后兼容）：`place.confidence`（坐标置信度）、`place.source`（来源标注：攻略文本/截图/手填）。

---

## 6. 已有资产 → 新方案映射

| 资产 | 新方案中的角色 |
| --- | --- |
| React 编辑器（拖拽/跨天/编辑表单/repick/撤销栈，探针 18/18+4/4） | **核心组件**：AI 改 + 手动改共用的把关层 |
| mapCore 纯函数（Mercator 箭头红线/投影中点） | 直接复用 |
| geocode 三级降级 + confidence | 攻略提取的店名补坐标，幻觉兜底链路 |
| 自包含 HTML 导出（后端 builder + 模板） | 分享/带走能力，不变 |
| C-1 单进程（API+前端同源） | 本地优先形态的载体 |
| BYOK 设置面板 | 待建（M12） |
| 对话 UI + 攻略提取 prompt + 对话改路线 | 待建（M13/M14 主战场） |

---

## 7. 里程碑（M12 起为刀切建设，每刀可演示）

- [x] M1-M6：旧版模板/Skill 时代（归档于 git 历史）
- [x] M7：FastAPI 后端（smoke 20/20）
- [x] M8：React 前端（探针 13/13）
- [x] M9：编辑器移植（探针 18/18 + DnD 4/4）
- [x] M10：坐标补全 + 搜索（三级降级）
- [x] M11：部署配置自动化 + C-1 单进程本地形态
- [ ] M12：BYOK 设置面板（设置弹窗/测试连接/视觉能力探测/localStorage）
- [ ] M13：对话 UI + 文本攻略提取（第一刀：贴文字 → 地图，主线闭环）
- [ ] M14：对话改路线（AI diff route → 即时重排 + 撤销集成）
- [ ] M15：截图解析（VLM 直出 route JSON）
- [ ] M16：链接解析（匿名 fetch → 可选 resolver 插件）
- [ ] M17：可选上云（HF Spaces / 国内 VPS，用现有 Dockerfile）

## 8. 边界（明确不做）

1. 不做账号体系、收藏云同步
2. 不抓取价格、不做预订
3. 不内置任何平台凭据；链接解析仅用用户自己的会话且为可选插件
4. 不做规划质量的无底军备竞赛——质量上限由「攻略已被作者验证」保证，产品价值在落地与修改
