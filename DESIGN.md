# IterTrip · 设计文档

> 状态：v1.0 方向定稿 · 2026-09-01 · 定位变更：独立 Web 应用（脱离 Skill 附庸）
> 产品一句话：**把你从任何地方看到的旅游攻略，变成一张可以动手改、可以带走的地图。**

---

## 1. 定位与叙事

### 1.1 产品定位（v1.0 变更）

旧定位（已废弃）：「AI 帮你规划旅行」——红海，与携程问道/飞猪问一问正面竞争，无胜算。

新定位：**攻略的结构化落地工具**。用户在任何内容平台（小红书/公众号/抖音/B站）刷到攻略，
人肉流程是「收藏 → 开地图逐个搜 → 自己排顺序 → 发现动线乱」。IterTrip 把这一步变成：
**贴进对话框 → 得到结构化路线 → 地图直观呈现 → 动手修改 → 导出带走**。

### 1.2 为什么有竞争力

- **大厂不占位**：OTA 的 AI 生成自家行程包，绝不会解析竞品平台的攻略——中立落地工具是空位
- **LLM 能力对齐**：「非结构化 → 结构化」恰是大模型成本最低、成功率最高的任务；攻略里的地点已被作者验证过，比凭空规划可靠
- **差异化在生成之后**：市面 AI 规划器全是「生成一次就死」的静态结果；本产品的核心闭环是
  **对话改路线（AI 动手）+ 编辑器（用户把关）+ 撤销栈（随时反悔）**——这个组合目前基本无人做
- **产物护城河**：自包含 HTML 导出（可编辑版）是既有招牌能力，分享即产品体验

### 1.3 形态决策（已定）

| 决策 | 结论 | 理由 |
| --- | --- | --- |
| 产品身份 | **独立 Web 应用** | 不再是任何 Agent/Skill 的附庸 |
| 部署形态 | **本地优先（C-1 单进程）** | start.ps1 一键起；断网可演示；将来可选上云 |
| LLM 配置 | **BYOK + 管理后台 + 免费源兜底** | 用户 key 存 localStorage；后端代理调用（规避 CORS 碎片化）；优先级 BYOK 头 > env > admin_config.json（可热更新）> .env 免费源 > mock |
| 输入方式 | **降级矩阵**（见 §3） | 贴文字/截图是主路径；链接解析是 v1 后扩展（见 §9） |
| 模型策略 | **单多模态模型全包** | 一个 VLM 干「攻略理解 + 提取 + 对话改路线」全部工作，不做规划/视觉双模型 |
| 抓取边界 | 用户自己的会话/key/账号 | 工具只是壳；不内置任何平台凭据 |

---

## 2. 核心体验流

```
用户打开应用
    ↓
对话框：「想去成都 3 天」 / 贴一段小红书攻略文字 / 丢一张攻略截图 / 贴一个链接
    ↓
AI 理解意图 → 生成/提取 route JSON（schema 见 §5）
    ↓
地图直观呈现（图钉 + 按天色板 + 有向路线 + 比价卡）
    ↓
两条修改路径并存：
  ① 对话改：「第二天太赶，博物馆挪到第一天下午」→ AI diff route → 即时重排
  ② 手动改：拖拽排序 / 跨天移动 / 编辑表单 / 地图改点 / 撤销重做（已实现）
    ↓
导出：自包含可编辑 HTML / route JSON
```

**关键交互原则**：AI 是动手的，用户是把关的。AI 的每次修改走同一历史栈，可撤销；
AI 修改前在对话里说明「我改了什么」，地图高亮变化处。

---

## 3. 输入降级矩阵（可靠性设计核心）

| 优先级 | 输入方式 | 实现 | 可靠性 |
| --- | --- | --- | --- |
| 主路径 ① | 粘贴攻略文本 | 纯文本 → LLM 提取 | 永不坏 |
| 主路径 ② | 上传/粘贴截图 | VLM 直接看图出 route JSON（一次调用，不做图→文中转）**（M15 已接入：≤4 张、前端压缩、原图不进后续上下文）** | 稳定（依赖 vision 模型；探测不支持时前端置灰） |
| 兜底 ③ | 纯对话 | 意图收集 → 生成 | 永不坏 |
| 扩展 ④ | 贴平台链接（**v1 后做，见 §9**） | 三层降级：a) 匿名 fetch 分享短链；b) 失败→提示改用 ①②；c) 可选 resolver 插件（Playwright + 用户扫码，本地缓存会话，参考 MediaCrawler 思路但**仅作可选扩展、不进核心依赖**——其学习用途 license 不允许打包分发） | 随平台风控波动 |

**红线**：④ 永远是 best-effort。爬虫被掐死时，产品一条腿都不断。v1 不投入，作为后置扩展决策。

---

## 4. 模型与 BYOK 设计

### 4.1 模型配置（BYOK 设置面板 + 服务端管理后台）

**BYOK 设置面板**（前端，localStorage 持久化）：

- 字段：Base URL / API Key / 模型名（下拉 + 自由输入）+「测试连接」按钮
- 测试连接 = 1 token 补全探测；顺带探测**是否接受图片输入**，不接受则置灰截图入口（M15 已接线：运行中被拒也会自动回写置灰）
- 推荐 VLM 优先（DeepSeek 无视觉，默认供应商将变更；候选：Qwen-VL 系 / GLM-V 系 / GPT-4o / Gemini-Flash 系）

**管理后台**（M-Admin-1，`/admin`，`ITERTRIP_ADMIN_TOKEN` 保护，未配置即整体关闭）：

- 服务器端免费 AI 服务的 Key/模型/启停在线管理与实时探测；配置存 `admin_config.json`（gitignore，热更新）
- Key 脱敏展示（sk-***1234）；PUT 时留空 = 保留原值；token 常量时间比较

**解析优先级（全链路统一）**：BYOK 请求头 > env（ITERTRIP_LLM_*）> 管理后台配置 > .env 免费源（ITERTRIP_FREE_*）> mock 降级。

### 4.2 调用链路

前端请求头带 `X-LLM-Base / X-LLM-Key / X-LLM-Model` → 本地后端代理转发。
为什么不让浏览器直连供应商：CORS 政策每家不同且随时会变（Anthropic 需专用头、DeepSeek 未承诺），
后端代理用 OpenAI 兼容格式通吃，planner.py 已验证。key 虽经过本地进程但不出用户机器。

### 4.3 上下文与成本护栏

1. 截图提取完成后，原图从后续轮次上下文丢弃（只留结构化结果），防 token 膨胀
2. JSON 校验失败 → 带错误重试一次（pydantic 校验即修复环地基）
3. 对话历史裁剪：只保留最近 N 轮 + 当前 route 快照，不无限累积
4. 记忆检索注入硬上限：top-k ≤ 6 条且总字数 ≤ 1200（见 §4.4）
5. 决策轨迹上限：单轮 ≤ 60 步，随消息进 localStorage（见 §4.5）；轨迹只含决策摘要，不含 prompt/响应原文与任何 key

### 4.4 长期记忆与检索增强（M18，opt-in）

对话代理的**长期记忆**：RAG 是实现手段，产品叙事是「攻略库越用越懂你」。

- **落点 A 攻略记忆库**：每次成功提取的攻略按**实体级**切分（`place_card` 地点原子事实 /
  `trip_summary` 整篇级）→ 本地 embedding（bge-small-zh ONNX）→ SQLite 单文件（`memory.sqlite`）；
  下次对话提到同一目的地时检索 top-k，拼【记忆参考】块注入 user payload，模型**带 [n] 引用**回答
- **落点 B 坐标实体记忆**：用户在地图上手动确定/修改的坐标即 ground truth，入 `place_entity`；
  geocode 降级链**第 0 级**命中同名同城即直接返回 confidence=high（省一次 LLM 调用，消灭小店坐标幻觉）
- **检索实现**：SQL 预过滤（档案 + 城市 + chunk 类型）后候选集内暴力余弦。单用户几百条量级 <1ms，
  引入 sqlite-vec/FAISS 的编译与兼容成本不值（代码留 `VectorIndex` 接口位，是 YAGNI 自觉）
- **隐私**：默认关闭（`ITERTRIP_MEMORY_ENABLED=0`）；开启后按匿名档案（前端随机 id，请求头
  `X-Traveler-Id`）隔离，不同档案互不可见；设置面板可一键清空自己的全部记忆。攻略原文由
  「只在本机浏览器」变为「进服务器库存」，所以必须显式 opt-in

### 4.5 决策可见化、坐标可信度与事实校验（M19 + M20 + M22）

AI 的过程不该是盲盒：用户要看得见「这一轮用了谁、依据什么、动了哪里」。

- **决策轨迹**：`/api/chat` 新增 `event: trace`，逐步下发 provider（模型与来源）/ memory（命中或未注入原因）/
  llm（首字延迟、字数）/ retry / geocode（每个地点的坐标出处与动作）/ edit / summary；终帧 `reply` 带完整
  `trace` + `stats`，前端渲染成可折叠「🧭 决策过程」并**随消息持久化**（刷新后仍能重放）。
  原则：**静默兜底必须变成可见决策** —— 历史上一次漏 import 让坐标基准转换静默失败了很久。
- **坐标可信度（geocode 降级链重排，M19）**：记忆真值 → **高德 POI（升为一级源：多候选打分 + 城市校验 +
  请求级缓存/熔断）** → 模型知识 → 搜索兜底 → 城市中心。对已有坐标再做**主动核验**，
  **用户手点的位置永不被覆盖**。
- **区域校验与「精修不搬家」（M20）**：M19 的核验缺了地理合理性判断，实测把一条长沙行程的 7 个地点
  搬到了山西/河北/北京/河南/天津（根因：目的地整串 `湖南·长沙` 被当高德 `city` 参数，高德不认便
  **静默忽略 citylimit** 返回全国结果，而 `citylimit` 之外那一步的城市校验是死代码）。
  现在：① 目的地先解析成 (省, 市)，只把**城市名**传给高德；② 候选必须省份/城市命中或距目的地城市中心
  ≤200km 才参与打分；③ 强匹配且偏差 ≤1.5km 直接对齐，>1.5km 需**精确同名或候选池 ≥3 条互相印证**
  才允许搬，否则保留原坐标 + 标【坐标待确认】；④ 紧凑行程加**地理包络**上限（现有坐标本身已离谱时
  不受保护，保证「整条路线都错」也能修）；⑤ 置信度跟随匹配分，弱匹配/模糊命中不再冒充「高德核验」。
- **地点自主改回（M21）**：判据从「行程内部是否自洽」升级为「**与用户要求的目的地是否一致**」。
  目的地参照点 = 内置城市表（46 城）→ 表外城市退到省会；当某个点与「高德说这个地点所在的位置」
  相差 >200km（同城偏差不可能是这个量级），且改回去后**更靠近参照点**，就判定它被放错了地方并
  主动改回（记 `redirect`）。同时收紧跳过条件：`amap/high` 标签不再单独构成「已核验」，
  必须**标签 + 位置自洽**同时成立才跳过 —— 否则历史遗留的错误坐标永远不会被重新审视。
  这是**默认路径**（每次生成/改路线都跑），不需要用户点任何按钮；合法远点（长沙行程里的张家界）
  因「不会更靠近参照点」而被保护，用户的自主修正仍可撤销。
- **溯源**：`place.source`（memory/user/amap/llm/search/city）+ `place.confidence` 落到 route JSON，
  时间线与地图气泡显示来源徽标；编辑器提供「🔍 按名称重新定位」与手动选点两条纠错路径。
- **规划页对话留存**：`itertrip:planchat`（按行程指纹隔离）：刷新恢复、换行程自动开新会话、可一键清空。
- **整条重校准（M20）**：规划页「🔍 校准坐标」→ `POST /api/route/recheck`，对历史遗留的坏坐标/缺坐标
  重跑补全与核验（`force_verify` 不信任历史来源标注，用户真值跳过），结果走同一条撤销栈。
- **事实校验：出发日期与闭馆日（M22）**：坐标之外，`ticket`/`time`/`note` 此前**零核验、零溯源**。
  第一个落地的切片是**闭馆日冲突**：真实交付物把「周一闭馆」的谢子龙影像艺术馆排在了 D5 = 2026-10-05 = 周一，
  而 `trip.dates` 是自由文本（「10月1日下午 – 10月6日晚上（国庆假期）」）连年份都没有，算不出星期。
  故新增结构化 `trip.start_date` + `date_source`：完整日期=用户给定，只有月日或阳历节日名则**就近推断**
  （「国庆」→ 今年 10/1），界面**显式标注「推断」、点日历即可改**（走同一条撤销栈）。
  判据是 `(周|週|星期|礼拜) + 星期几 + 闭馆词` 的文本解析 ↔ 该天 weekday（D1 = 出发日），
  命中即写 `place.warnings`（**独立字段，不污染 `note`**）。**不调模型、不发网络请求**；
  无日期时如实显示「未检查闭馆日」而不是假装通过。理由与边界见 `docs/M22_FACT_CLOSURE_PLAN.md`。

---

## 5. 数据模型（不变项）

route JSON 契约维持现状（trip / days[] / places[] / hotel / summary），
它是「生成、编辑、导出」三端共享的唯一数据源，也是 AI 对话修改的操作对象。
新增可选字段（**M19 已落地**，向后兼容）：`place.confidence`（坐标置信度）与 `place.source`
（坐标出处：memory/user/amap/llm/search/city）；前端据此渲染来源徽标，老路线缺字段按「未标注」处理。
**M22 追加**（同样可选、向后兼容）：`trip.start_date`（YYYY-MM-DD，给机器算星期）+ `trip.date_source`
（user/inferred）+ `place.warnings[]`（确定性事实告警，如「闭馆日：周一闭馆，当天为周一」）。

---

## 6. 已有资产 → 新方案映射

| 资产 | 新方案中的角色 |
| --- | --- |
| React 编辑器（拖拽/跨天/编辑表单/repick/撤销栈，探针 18/18+4/4） | **核心组件**：AI 改 + 手动改共用的把关层 |
| mapCore 纯函数（Mercator 箭头红线/投影中点） | 直接复用 |
| geocode 六级降级（记忆 → 高德 POI → 模型 → 搜索 → 城市中心）+ confidence + source | 攻略提取的店名补坐标；M19 起高德为一级源并主动核验，M20 起候选须过地理闸门（省市 / ≤200km）且核验「精修不搬家」，来源可溯源 |
| 自包含 HTML 导出（后端 builder + 模板） | 分享/带走能力，不变 |
| C-1 单进程（API+前端同源） | 本地优先形态的载体 |
| BYOK 设置面板 | **已建**（M12：设置弹窗/测试连接/localStorage，X-LLM-* 头贯通 plan/geocode/chat） |
| 对话 UI + 攻略提取 prompt + 对话改路线 | M13 **已建**（/api/chat 双模式）；M14 **已建**（页内抽屉 + diff 高亮 + 撤销集成） |

---

## 7. 里程碑（M12 起为刀切建设，每刀可演示）

- [x] M1-M6：旧版模板/Skill 时代（归档于 git 历史）
- [x] M7：FastAPI 后端（smoke 20/20）
- [x] M8：React 前端（探针 13/13）
- [x] M9：编辑器移植（探针 18/18 + DnD 4/4）
- [x] M10：坐标补全 + 搜索（三级降级）
- [x] M11：部署配置自动化 + C-1 单进程本地形态
- [x] M12：BYOK 设置面板（设置弹窗/测试连接/localStorage；视觉探测 UI 置灰随 M15 接入，/api/llm/test 已含探测）
- [x] M13：对话 UI + 文本攻略提取（贴文字 → 地图主线闭环；探针 15/15）
- [x] M14：对话改路线（规划页内嵌 AI 抽屉；前端 diff 叙述 + 新/移位点闪烁高亮 + 同一撤销栈；探针 12/12）
  - 落地要点：routeDiff.ts 同名配对 diff（added/removed/moved）；MapView data-pin-key 通道 + flash 动画 + panTo；useTripHistory 改同步快照（修复异步回调丢历史帧）
  - 难点提示：AI 改完的**变化可视化**比改路线本身更费工——新地点闪烁 / 移走的地点淡出 / 「我改了什么」的叙述。
  - 先行实验：在 UI 投入前，先用 prompt 实验确认模型能稳定输出「修改描述 + 完整 route JSON」的结构化格式。
- [x] M15：截图解析（VLM 直出 route JSON：/api/chat images[] ≤4 张、前端 canvas 压缩长边 2400px、原图不进持久化历史与后续轮次；[vision-unsupported] 自动置灰入口；实测免费源可看图出正确路线）
- [x] M17：Agent 式澄清问答（need_more_info + 结构化 questions[]，type 含 text/select/multi/date，前端澄清卡 + 日历/自定义选项）
- [x] 管理后台（M-Admin-1）：/admin 免费服务配置热更新 + 探测 + token 鉴权 + key 脱敏
- [x] 体验与导入导出补全：流式等待计时 + 分时段提示 + 思考链强制展开；HTML 导出根因修复（中文文件名 RFC 5987 双写）+ 导出副本坐标清洗（无「非洲点」）；首页导入（JSON / 导出 HTML 往返）
- [x] M16：上云（已在自有腾讯云服务器完成自部署验证：C-1 单进程 + Nginx 子路径 + `.env` 配置）
- [x] M18：旅行记忆库（RAG，opt-in）：攻略实体级切分 → 本地 embedding（bge-small-zh ONNX）→
      SQLite 单文件向量检索（城市元数据预过滤）→ 带引用注入；坐标实体记忆作 geocode 第 0 级；
      匿名档案隔离 + 设置面板一键清空
- [x] M19：坐标可信度 + 决策可见化 + 规划页对话留存
  - 修复两个基准 bug：提取路径不转坐标系、改路线转换漏 import 被静默吞掉（实测固有偏移 361m~555m）
  - geocode 重排为 记忆 → **高德 POI（一级）** → 模型 → 搜索 → 城市中心；对已有坐标主动核验
    （实测模型坐标偏真实 POI 113m~1.2km，核验后直接对齐；弱匹配不挪动、用户真值永不覆盖）
  - `place.source/confidence` 溯源 + 时间线/地图气泡来源徽标 + 编辑器「按名称重新定位」
  - `event: trace` 决策轨迹（provider/memory/llm/retry/geocode/edit/summary）+ 前端「🧭 决策过程」可折叠块
  - 规划页 AI 抽屉对话与轨迹持久化（`itertrip:planchat`，按行程指纹隔离）
- [x] M20：坐标区域校验 + 整条重校准（事故驱动）
  - 修复「目的地整串（`湖南·长沙`）被当高德 `city` 参数 → `citylimit` 被静默忽略 → 同名异地 POI 被当命中」：
    实测一条长沙行程的 7 个地点被搬到山西/河北/北京/河南/天津；副因是城市中心表无长沙（3 个点最终 0,0）
  - 候选地理闸门（省份 / 城市 / 距目的地城市中心 ≤200km）+ 只把解析出的城市名传给高德
  - 核验收紧为「精修不搬家」：≤1.5km 直接对齐；>1.5km 需精确同名或候选池 ≥3 条印证；否则只标注
  - 行程地理包络 + 现有坐标已离谱时不受保护（保证整条路线都错的行程也能修）；城市中心表补到 46 城
  - 模糊兜底捞回错别字店名（笨萝卜↔笨罗卜），置信度跟随匹配分（弱匹配不再冒充「高德核验」）
  - 新增规划页「🔍 校准坐标」与 `POST /api/route/recheck`，让历史遗留的坏行程能就地修好（走同一撤销栈）
- [x] M21：地点自主改回（判据锚定「用户要求的目的地」）
  - 目的地参照点 `destination_anchor()`：城市表 46 城 → 表外退到省会（`_PROVINCE_CENTER` 34 项）→ 无则不做地理否决
  - 第三条独立佐证：现有坐标与高德所说位置相差 >200km 且改回后更靠近参照点 → 自主改回（`redirect`）
  - 跳过条件收紧：`amap/high` 必须**同时**位置自洽（离目的地 ≤200km）才跳过；正常行程 0 次额外请求
  - 实测：整条路线都在别的省（中位数也错）时 7/7 自主改回；真实行程默认路径 31 次请求 / 写入 21 处 /
    距目的地 >300km 从 7 → 0；合法远点（张家界）不被同名异地 POI 搬走
- [x] M22：出发日期推断 + 闭馆日冲突检查（事实层第一刀）
  - 事故：真实交付物把「周一闭馆」的谢子龙影像艺术馆排在 D5 = 2026-10-05 = 周一；
    后端全仓无 weekday 逻辑，`trip.dates` 又是自由文本连年份都没有 → 系统毫无察觉
  - 新增 `backend/engine/facts.py`：闭馆规则解析（简繁 + 例行 + 节假日除外）+ 日期就近推断 + 逐天 weekday 比对
  - 契约：`trip.start_date`/`date_source`（推断时 UI 标「推断」并可一键改）/ `place.warnings[]`
  - 新增 `POST /api/route/datecheck`；规划页日期卡 + 时间线/地图/导出 HTML 三处告警渲染
  - 实测：真实交付物恰好 1 处冲突（D5）；改成 2025-10-01 归零；`check_facts` 10/10 + 探针 19/19 + 导出探针 9/9

## 8. 边界（明确不做）

1. 不做账号体系、收藏云同步
2. 不抓取价格、不做预订
3. 不内置任何平台凭据；链接解析仅用用户自己的会话且为可选插件（v1 后扩展，见 §9）
4. 不做规划质量的无底军备竞赛——质量上限由「攻略已被作者验证」保证，产品价值在落地与修改

## 9. v1 后扩展（暂不做，待用户反馈决定）

- [ ] 链接解析（匿名 fetch → 可选 resolver 插件）：产品核心价值已由 ①②③ 输入闭环覆盖，且与「不内置平台凭据」存在配置门槛张力。等 v1 上线、确认真实需求后再决定是否投入。
