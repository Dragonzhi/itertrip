fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
# M18 旅行记忆库（RAG）可选依赖：本地 embedding（bge-small-zh ONNX）。
# 默认不启用记忆库，故不强制安装；开启 ITERTRIP_MEMORY_ENABLED=1 且 provider=local 时取消下一行注释：
# fastembed>=0.3
