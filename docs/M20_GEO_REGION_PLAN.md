# M20 · 坐标区域校验与「校准坐标」实施记录

> 版本：v1.0 · 2026-09-06 · 适用代码库：`backend/`（FastAPI）+ `frontend/`（React 18 + Vite）
> 触发来源：用户反馈「这是最新的规划，地点的位置完全失真啊！」并附上一份真实导出 JSON
> 结论：**M19 引入的「高德核验」存在一个没有地理校验的漏洞，把一条长沙行程的 7 个地点搬到了
> 山西/河北/北京/河南/天津。本里程碑修复根因，并新增「🔍 校准坐标」让已经坏掉的行程能就地修好。**

---

## 1. 事故复盘

### 1.1 现场

用户提供的 `国庆长沙6日游：山水洲城与烟火湘味.json`（25 个地点）中，7 个地点的坐标落在**别的省**：

| 地点 | 事故坐标 | 实际所在 | 高德全国检索的第一名 |
|------|----------|----------|----------------------|
| 五一广场 | 37.8615, 112.5739 | 长沙芙蓉区 | 山西太原 迎泽区 |
| 天心阁 | 38.3064, 116.8546 | 长沙天心区 | 河北沧州 运河区 |
| 费大厨辣椒炒肉（悦方/国金街店） | 39.9660, 116.4108 | 长沙芙蓉区 | 北京 东城区 |
| 文和友（海信广场店） | 39.5230, 116.7059 | 长沙天心区 | 河北廊坊 广阳区 |
| 毛泽东铜像广场 | 36.1595, 114.6376 | 湘潭 韶山市 | 河南安阳 |
| 毛家饭店（韶山冲） | 40.0466, 117.2410 | 湘潭 韶山市 | 天津 蓟州区 |
| 湖南米粉街（韭菜园） | 39.1173, 116.0252 | 长沙芙蓉区 | 河北保定 高碑店 |

另有 3 个地点（笨萝卜浏阳菜馆、炊烟时代小炒黄牛肉、特产采购）坐标是 `0,0`。

### 1.2 根因

**R1（主因）· `city` 参数传了高德不认识的东西，`citylimit` 被静默忽略。**
`geocode_amap_scored(name, city=destination)` 把目的地字符串**原样**当城市名传给高德。
这份行程的 destination 是 `湖南·长沙`——高德不认识省市连写，**不报错，直接忽略 `citylimit`，
返回全国结果**。实测：`city="湖南·长沙"` 查「天心阁」→ 河北沧州；`city="长沙"` → 湖南长沙天心区。

**R2 · 本该拦住它的城市校验是死代码，而且判据本身是错的。**
`_pick_best_poi` 的 `require_city` 只写在**第二步**（去掉 citylimit 的兜底查询）里，
第一步完全没有校验；而它的判据是 `_norm_name("湖南·长沙") in _norm_name("湖南省 长沙市 天心区")`
→ `"湖南长沙"` 永远不是 `"湖南省长沙市天心区"` 的子串 → 即使执行也**会拒绝一切**，包括正确的 POI。
第一步于是把「五一广场（太原）」按名称完全相同给了 1.0 分，直接当作命中。

**R3 · 城市中心表没有长沙，最后一级兜底整条失效。**
`_CITY_CENTER` 只有 12 个城市，长沙不在其中；`geocode()` 第 4 级用 `city_name in city` 匹配，
`"成都"` 不在 `"湖南·长沙"` 里 → 三个非 POI 条目（「特产采购」之类）拿到 `lat/lng = 0`。

**R4 · 「强匹配直接采用」在缺少地理校验时，反而破坏了本来正确的坐标。**
M19 的核验策略是「名称强匹配（≥0.85）→ 直接采用高德 POI 坐标」。
五一广场、天心阁在模型输出里本来就是同城正确的（只差几百米），
正是这一步把它们搬到了太古/沧州。**一次没有地理合理性校验的「核验」，比不核验更糟。**

---

## 2. 修复方案

### 2.1 目的地解析 `parse_region()`（`coordinates.py`）

把目的地拆成 `(省份列表, 城市列表)`，都做「去行政区后缀」归一：

| 输入 | 输出 |
|------|------|
| `湖南·长沙` | `(['湖南'], ['长沙'])` |
| `湖南省长沙市` / `湖南长沙` / `长沙市` / `长沙` | `(['湖南'], ['长沙'])` / `([], ['长沙'])` … |
| `成都·都江堰` | `([], ['成都', '都江堰'])` |
| `北京` | `(['北京'], ['北京'])`（直辖市既是省也是市） |
| `云南` | `(['云南'], [])` |
| 解析不出（境外 / 泛指） | `([], [])` → 调用方**不做区域否决** |

### 2.2 候选地理闸门 `_region_ok()`

每个候选 POI 必须满足**任一条**才允许参与打分：

1. **城市命中**：候选的 `pname/cityname/adname` 归一后包含目的地的某个城市名；
2. **省份命中**：包含目的地的某个省份（覆盖「长沙行程里的韶山」这类跨城一日游）；
3. **距目的地城市中心 ≤ 200km**（`_AMAP_MAX_DRIFT_KM`，覆盖韶山 65km / 乐山 140km / 峨眉山 155km）；
4. 兜底不否决：目的地解析不出省市、或候选缺城市字段（没有判断依据时不构成反对）。

### 2.3 传给高德的 `city` 只取城市

`city_param = 城市[0] or 省份[0] or ""`。实测**省份也能被高德当粗过滤用**（`city=湖南` 查
「五一广场」返回邵阳的同名点，而不是太原的）；只有省市连写才是无效值。

### 2.4 核验策略：精修，不搬家

| 情形 | 动作 | 记录 |
|------|------|------|
| 强匹配 ≥0.85 且偏差 ≤1.5km | 采用 POI 坐标（M19 实测模型坐标普遍偏 113m~1.2km） | `align` |
| 强匹配且偏差 >1.5km，**名称完全一致**（≥0.99） | 采用（精确同名是长距离替换的佐证） | `replace` |
| 强匹配且偏差 >1.5km，**候选池 ≥3 条互相同意**（0.5km 内） | 采用（例：谢子龙影像艺术馆的 5 条候选同点） | `replace` |
| 强匹配但**非精确同名**且偏差 >1.5km | **保留原坐标** + 标【坐标待确认】 | `conflict` |
| 弱匹配 0.72~0.85 且偏差 >1.5km | 保留原坐标 + 标【坐标待确认】 | `conflict` |
| 弱匹配但偏差 ≤1.5km | 一个字段都不动（不升级来源标注） | `confirm` |
| 用户真值（`source=user` / 实体记忆命中） | 永不参与 | — |

反例依据：`湖南博物院` 的高德第一名是「湖南博物院(南院)」（0.943 分，偏 2.3km），
而模型给的坐标离真实院址只差 0.4km —— 按分值强行采用只会更糟。

### 2.5 行程地理包络（第二道防线）

紧凑行程（有效点离中位数都 ≤100km）里，强匹配 POI 若落在
`max(spread, 50) + 150km` 之外 → 不采用，保留原值 + 标【坐标待确认】。

**例外**：现有坐标**本身已离谱**（离行程中位数 >100km 或离目的地城市中心 >200km）时不做保护，
也不拿它当「就近裁决」的参考点 —— 否则「整条路线都在错误省份」时一个点都修不回来
（这正是「校准坐标」按钮首轮探针失败的原因，见 §3.3）。

### 2.6 就近裁决与分支名打分

- **分支名**：`_candidate_score` 取「清洗名」与「完整名（含括号）」两次打分的**较大值**。
  `火宫殿（坡子街总店）` 因为括号内容匹配上「火宫殿(坡子街总店)」而拿到 1.0，
  否则只会按名字长度比选中 2.3km 外的「火宫殿(五一路店)」。
- **并列裁决**：分数差 ≤0.05 视为同名多分店，取离「该地点现有坐标（核验场景）」或
  「目的地城市中心（补全场景）」更近的那个 —— 游客行程里离目的地近的那家才是有意去的那家。

### 2.7 模糊兜底 + 置信度跟随匹配分

- `_char_overlap`（字符集合 Jaccard，候选名同样先剥括号）用于**严格打分全灭**时兜底：
  模型把「笨罗卜浏阳菜馆」写成「笨萝卜浏阳菜馆」、「炊烟小炒黄牛肉」写成「炊烟时代小炒黄牛肉」——
  严格打分是 0 分，但这些店真实存在于目的地。阈值 0.65，分数**钳在 0.80 以下**（低于强匹配阈值），
  所以只用于「本来就没坐标」的补全，永远不参与「覆盖已有坐标」。
- `geocode()` 的高德分支置信度改为**跟随匹配分**：≥0.85 → `high`（时间线显示「高德核验」），
  否则 `low`（显示「AI 推测」并追加【坐标待确认】）。弱匹配不再冒充核验。

### 2.8 城市中心表补全

`_CITY_CENTER` 从 12 个城市扩到 46 个（含长沙、武汉、天津、青岛、昆明、贵阳、南宁、郑州、济南、
福州、哈尔滨、沈阳、大连、石家庄、太原、兰州、西宁、银川、呼和浩特、乌鲁木齐、拉萨、海口、三亚、
桂林、丽江、张家界、宁波、无锡、珠海、香港、澳门、台北等），并按 `parse_region` 的结果精确查表。

### 2.9 「🔍 校准坐标」按钮 + `POST /api/route/recheck`

历史遗留的坏路线不能只靠「重新生成」来修。新增：

- 后端 `planner.recheck_route()` → `POST /api/route/recheck`，对整条路线重跑
  **阶段①离谱检测 + 阶段②缺失补全 + 阶段③主动核验**，`force_verify=True`：
  **不信任历史来源标注**（坏掉的核验恰好把来源标成 `amap/high`，正是跳过核验的条件）。
  用户手改真值仍然跳过；不做坐标系转换（输入输出都是已存的 GCJ-02）。
- 前端规划页工具条「🔍 校准坐标」：结果走 `mutate()` 进**同一条撤销栈**（可 Ctrl+Z 反悔），
  完成后提示「已校准/补全 N 处坐标；M 处存疑已标【坐标待确认】」。

---

## 3. 实测

### 3.1 真机回放这份长沙行程（修复后）

`filled=21`、高德请求 36 次、未熔断、**离长沙市 >300km 的点：0**。

| 地点 | 修复前 | 修复后 | 移动 |
|------|--------|--------|------|
| 五一广场 | 37.8615, 112.5739 | 28.1965, 112.9773 | 1075.4km |
| 费大厨辣椒炒肉（悦方/国金街店） | 39.9659, 116.4108 | 28.1927, 112.9789 | 1346.4km |
| 天心阁 | 38.3064, 116.8546 | 28.1848, 112.9819 | 1181.4km |
| 文和友（海信广场店） | 39.5230, 116.7058 | 28.1891, 112.9697 | 1306.3km |
| 毛泽东铜像广场 | 36.1595, 114.6376 | 27.9038, 112.4872 | 940.0km |
| 毛家饭店（韶山冲） | 40.0466, 117.2410 | 28.2057, 112.9720 | 1373.5km |
| 湖南米粉街（韭菜园） | 39.1173, 116.0252 | 28.1964, 112.9882 | 1246.2km |
| 笨萝卜浏阳菜馆 | 0, 0 | 28.1905, 112.9777 | 新补（`amap/low`） |
| 炊烟时代小炒黄牛肉 | 0, 0 | 28.1936, 112.9795 | 新补（`amap/low`） |
| 特产采购（茶颜悦色周边/黑色经典） | 0, 0 | 28.1907, 112.9879 | 新补（`city/low`） |

同时验证了「不再过度修正」：

| 地点 | 结果 | 说明 |
|------|------|------|
| 火宫殿（坡子街总店） | 28.1906, 112.9738（0.7km） | 括号分支名打分选中坡子街总店，不是 2.3km 外的五一路店 |
| 文和友（海信广场店） | 28.1891, 112.9697（0.7km） | 同上；此前并列裁决会选到「跳海美食城」 |
| 笨萝卜浏阳菜馆 | 28.1905, 112.9777（1.0km） | 全国/同城候选里选离五一广场最近的育英街店 |
| 滴水洞 | 27.9190, 112.4649（0km，`conflict`） | 高德在长沙有一个同名的「春风德意滴水洞」，包络保护住了韶山的正确坐标 |
| 湖南博物院 | 28.2153, 112.9913（0km，`conflict`） | 拒绝被「湖南博物院(南院)」（0.943 分、2.3km）带偏 |
| 谢子龙影像艺术馆 / 李自健美术馆 | 各修正 2.1/2.2km | 5 条候选落在同一点 → 候选池一致 → 允许长距离修正 |

### 3.2 已知残留（诚实记录）

- **毛家饭店（韶山冲）**：高德的全国候选里没有「韶山冲店」（只有天津/北京的异地同名店），
  最终落到长沙的同品牌分店（同省、约 60km）。**这是数据源覆盖问题，不是逻辑问题**；
  界面有来源徽标 + 「🔍 按名称重新定位」+ 手点覆盖，用户一次修正即成为真值。
- **橘子洲景区 / 湖南大学·登高路小吃 / 潮宗街历史文化街区**：高德没有足够强的对应 POI，
  保留模型坐标（同城，误差在百米~公里级），不强行挪动。
- **同名连锁分店**：即使有括号分支名与就近裁决，也无法保证选到用户心里那一家；
  这是数据源固有歧义（M19 已记录）。

### 3.3 测试

| 用例 | 结果 | 覆盖 |
|------|------|------|
| `check_geocode.py` [1]~[15] | 15/15 | 新增 [11] 区域解析、[12] 地理闸门（事故复现点为 `None`）、[13] 模糊兜底+就近裁决+置信度、[14] 行程包络、[15] 整条路线错城时也可修复 |
| `probe_recheck_ui.cjs` | 8/8 | 浏览器真机：坏坐标 → 点「🔍 校准坐标」→ 坐标改回长沙天心阁、来源 `amap`、撤销可还原、零页面错误 |
| `probe_recheck_http.py` | 通过 | 对真实 JSON 调 `POST /api/route/recheck`，逐点核对（§3.1 表格即其输出） |
| `probe_trace` / `probe_mapsettings` / `probe_phase3_dnd` | 16/16 · 10/10 · 4/4 | M19 决策轨迹与来源徽标、地图设置、拖拽（证明新按钮与徽标没破坏既有交互） |
| `check_trace` / `check_memory_api` / `schema_e2e` / `export_filename` / `check_m15` / `check_import` / `routeImport` / `check_memory` / `smoke_m15` / `smoke_m19` / `api_smoke`(22/22) | 全绿 | 完整回归，见 `test-artifacts/m20_regression.log` |
| `tsc -b && vite build` | 通过 | 前端类型检查 + 生产构建（394.01 kB / gzip 123.51 kB） |

> 首轮 `probe_recheck_ui` 曾失败（6/8）：单点行程里「行程包络」把修复也挡住了。这不是测试写错，
> 而是修复本身的缺口 —— 由此补出 §2.5 的「已离谱的点不受保护」与 `check_geocode[15]`。
>
> 回归中另有两项**测试基建腐化**（非本里程碑引入），一并修好并复跑通过：
> ① `check_schema.py` 缺 `sys.path` 引导，直接 `python test-artifacts/check_schema.py` 会
> `ModuleNotFoundError: backend`（此前靠运行方式侥幸通过）；
> ② `smoke_m18.py` 的夹具 prompt 缺日期/预算/人数，而免费源模型改版后会先反问澄清
> （返回 `need_more_info` + `questions[]`、intent=chitchat、无 route）→ 补齐字段后
> 「提取 3 地点 → 入库 4 条 → 二次对话命中注入并引用 [1][2]」全链路通过，
> 且断言失败时会打印 intent/questions/reply，下次一眼可定位。

---

## 4. 参数与契约

| 参数 | 值 | 位置 | 说明 |
|------|-----|------|------|
| `_AMAP_MAX_DRIFT_KM` | 200 | `coordinates.py` | 无省市信息时，POI 距目的地城市中心的最大容忍距离 |
| `_AMAP_FUZZY_SCORE` / `_AMAP_FUZZY_CAP` | 0.65 / 0.80 | `coordinates.py` | 模糊兜底阈值与分数上限（上限低于强匹配阈值） |
| `_AMAP_TIE` | 0.05 | `coordinates.py` | 分数差在此内视为并列 → 就近裁决 |
| `_AMAP_EXACT_SCORE` | 0.99 | `coordinates.py` | 名称完全一致：唯一允许「搬家」的单项佐证 |
| `_AMAP_AGREE_KM` / `_AMAP_AGREE_MIN` | 0.5 / 3 | `coordinates.py` | 候选池一致性半径与条数 |
| `_VERIFY_KM` | 1.5 | `planner.py` | 对齐/搬家的分界（沿用 M19 实测值） |
| `_VERIFY_DRIFT_KM` | 150 | `planner.py` | 行程包络外扩量 |
| `_OUTLIER_KM` / `_OUTLIER_REPLACE_KM` | 100 / 10 | `planner.py` | 离谱检测（沿用 M19） |

契约变化：

- `geocode_amap_scored()` 返回值从 `(lat, lng, score)` 变为 **`(lat, lng, score, agree)`**（多返回候选池一致条数）；
  新增只读关键字参数 `near=(lat, lng)`（同名并列候选的就近裁决参考点）。
- `geocode()` 的高德分支置信度改为跟随匹配分（弱匹配 → `low`）。
- `_enrich_coordinates()` 新增 `force_verify: bool = False`。
- 新增 `POST /api/route/recheck`，请求 `{route}`，响应 `{route, filled, records, amap_calls, amap_reason}`。
- `parse_region()` / `destination_anchor()` 为 `coordinates.py` 新增公开函数。
- **无新增环境变量**：区域校验与「校准坐标」都不需要开关，有 `ITERTRIP_AMAP_KEY` 即生效。

---

## 5. 使用说明

- **已经坏掉的行程**（本里程碑之前生成的）：进规划页点工具条「🔍 校准坐标」，
  或用 `POST /api/route/recheck` 批处理；也可以导入 §3.1 的修正版 JSON。
- **新生成的行程**：无需操作，区域闸门与「精修不搬家」策略已默认生效。
- 无高德 key 时：区域闸门不存在（不调高德），行为与配置前一致；
  但城市中心表补全仍然生效（表外城市不再返回 `0,0`）。
- 高德配额：单点最多 2 次请求，单次路线预算 40 次、熔断规则不变。
  校准一条 25 地点的行程实测 36 次请求。

---

## 6. 偏差说明

1. **M19 的文档口径已同步修正**：`AGENTS.md`（§4.C 降级链表 / 主动核验块 / §6.4 端点 / §7.3 参数 /
   §9 约束 / §10 路线图 / 用例 9，版本升 v1.3）、`DESIGN.md`（§4.5 / §6 / §7）、`README.md`、
   `README.en.md`、`DEPLOY.md` 中「强匹配直接采用 POI 坐标」的表述已改写为
   「≤1.5km 直接对齐；更远需精确同名或候选池一致」，避免文档与代码再次不一致。
2. **`_AMAP_STRONG_SCORE` 双份定义**已消除：`planner.py` 不再自己定义 0.85，
   统一从 `coordinates.py` 导入（`_AMAP_STRONG_SCORE` / `_AMAP_EXACT_SCORE` / `_AMAP_AGREE_MIN` / `_AMAP_MAX_DRIFT_KM`）。
3. **静默失败可见化**：`geocode_amap_scored()` 的兜底 `except` 现在会打印一行
   `[amap] 查询异常（按未命中降级）: ...`。本次开发中它曾把一个「下标取错」的编码错误
   静默降级成「高德全线无结果」，与 M19 记录的教训同类。
4. **`_CITY_CENTER` 坐标精度**：城市中心按市政府/市中心近似（WGS84，±1km 量级），
   只用于最低一级兜底，不参与精确核验。
5. **未改**：`memory_store._city_match`（子串包含已经能容忍 `湖南·长沙` ↔ `长沙`）、
   提示词、导出模板、M18 记忆链路。
